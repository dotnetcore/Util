@page
@model TextBoxModel
@functions {
    /// <summary>
    /// 基础表单
    /// </summary>
    public class TextBoxModel : PageModel { }
}
<page-header title="基础表单">
    将一个冗长或用户不熟悉的表单任务分成多个步骤，指导用户完成。
</page-header>
<nz-card>
    <form #form="ngForm" nz-form [nzLayout]="'inline'" (ngSubmit)="submitForm()">
        
    </form>
    <input nz-input placeholder="Basic usage" [(ngModel)]="model.value">
    <br>
    <br>
    <input nz-input placeholder="Basic usage" [(ngModel)]="model.name" [disabled]="true">
        
    <x-textbox placeholder="哈哈" [(model)]="model.value"></x-textbox>
</nz-card>
