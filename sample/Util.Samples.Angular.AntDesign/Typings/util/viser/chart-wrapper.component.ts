//============== Viser图表包装器 =====================
//Copyright 2019 何镇汐
//Licensed under the MIT license
//=====================================================
import { Component, Input, Output, Host, EventEmitter, Inject, forwardRef, OnInit, ContentChild, TemplateRef } from '@angular/core';
import { util } from "../index";
import { Model } from "./data/model";
import { DataSet } from '@antv/data-set';

/**
 * Viser图表包装器
 */
@Component( {
    selector: 'x-chart',
    template: `

    `
} )
export class ChartWrapperComponent {
   
}